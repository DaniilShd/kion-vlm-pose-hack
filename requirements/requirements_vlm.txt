# PyTorch для CUDA 12.8 (удаляем конкретные версии, они уже установлены в Dockerfile)
# torch
# torchvision
# torchaudio

transformers==4.45.0
accelerate==1.13.0
# bitsandbytes устанавливается отдельно в Dockerfile
safetensors==0.7.0
numpy==2.1.1
sentencepiece==0.2.0
gradio==5.0.0
aiofiles==23.2.1
pandas==2.3.3
tqdm